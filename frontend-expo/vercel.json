{
  "version": 2,
  "builds": [
    {
      "src": "frontend-expo/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "frontend-expo/web-build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
